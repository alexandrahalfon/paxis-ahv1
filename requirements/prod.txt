# Production dependencies for Paxis Backend
# Complete requirements for GCP Cloud Run deployment

# ===== Core Dependencies =====
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
email-validator>=2.1.1
tqdm>=4.65.0

# ===== API Framework =====
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
gunicorn>=21.0.0
python-multipart>=0.0.6
aiofiles>=23.0.0
passlib[bcrypt]>=1.7.4
python-jose>=3.3.0
bcrypt<4.0

# ===== AI/ML Libraries =====
openai>=1.0.0
mistralai>=0.0.8
sentence-transformers>=2.2.2
torch>=2.1.0
transformers>=4.35.2

# ===== Vector Database =====
qdrant-client>=1.7.0

# ===== Document Processing =====
pdf2image>=1.16.0
Pillow>=10.0.0
tiktoken>=0.5.0
nltk>=3.8.0

# ===== Data Processing =====
pandas>=2.0.0
numpy>=1.24.0

# ===== GCP Integration =====
google-cloud-storage>=2.10.0

# ===== HTTP Clients =====
httpx>=0.25.0
requests>=2.31.0

# ===== Background Processing (Optional) =====
celery>=5.3.0
redis>=5.0.0

# ===== Literature Search =====
biopython>=1.81
beautifulsoup4>=4.12.0
lxml>=4.9.0

# ===== Database =====
sqlalchemy>=2.0.0
asyncpg>=0.29.0

# ===== Monitoring and Logging =====
structlog>=23.0.0
sentry-sdk>=1.32.0

# ===== Performance =====
orjson>=3.8.0
ujson>=5.7.0

# ===== Report Generation =====
# report_service.py imports this unconditionally at module load — missing it
# crashes the entire app on boot, not just the PDF-report feature
reportlab>=4.0.0

# ===== PDF Processing (upload pipeline) =====
# document_processor.py (imported at module load by document_processing_service.py
# and user_uploads_service.py, both wired into main.py's upload routers) imports
# fitz unconditionally — same startup-crash risk as reportlab above
PyMuPDF>=1.23.0

# ===== Retry logic (upload/study-extraction pipeline) =====
# used by study_profile_extractor.py — was missing from every requirements
# file in the repo, not just prod.txt
tenacity>=8.2.0