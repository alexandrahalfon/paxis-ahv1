# ============================================
# Paxis Integrated Requirements
# Merged from Paxis (Enhanced RAG) + Paxis-2026 (Production)
# ============================================

# ===== Phase 1 & 2: Document Processing & Ingestion =====
mistralai>=0.0.8
openai>=1.0.0
pdf2image>=1.16.0
Pillow>=10.0.0
qdrant-client>=1.7.0
tiktoken>=0.5.0
nltk>=3.8.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
email-validator>=2.1.1
pandas>=2.0.0
numpy>=1.24.0
google-cloud-storage>=2.10.0
tqdm>=4.65.0
PyMuPDF>=1.23.0
reportlab>=4.0.0

# ===== Phase 3: Enhanced RAG (Our Implementation) =====
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
sentence-transformers>=2.2.2  # For cross-encoder reranking
torch>=2.1.0
transformers>=4.35.2
python-multipart>=0.0.6
httpx>=0.25.0
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
bcrypt<4.0

# ===== Phase 4 & 5: Production Features (Paxis-2026) =====
# Upload & File Management
aiofiles>=23.0.0
python-multipart>=0.0.6

# Background Processing
# celery / redis removed 2026-08-08: neither is imported anywhere in the
# codebase (verified by grep across src/ and scripts/). They were adding
# install time and image size for nothing. Re-add if a real task queue or
# shared cache is introduced — see BETA_SCALING_PROPOSAL.md, which
# proposes Redis for a cross-instance query cache.

# Literature Search & Alerts
biopython>=1.81          # PubMed access
beautifulsoup4>=4.12.0   # Web scraping
requests>=2.31.0
lxml>=4.9.0

# Production Server
gunicorn>=21.0.0
uvicorn[standard]>=0.24.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.30.0  # 0.30+ required for Python 3.13
alembic>=1.13.0  # migrations for exueed-patients — see migrations/patients_db/README.md
psycopg2-binary>=2.9.9  # sync driver for Alembic only; the app itself uses asyncpg

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Deployment
docker>=6.1.0
